# Contributing

Thanks for considering a contribution.

- Format code with `scalafmt` (or the repo's equivalent formatter) before opening a PR.
- Make sure CI passes.
- Keep PRs focused — one logical change per PR.

For anything repo-specific (build tool, running tests locally, etc.), check that
repository's README first.
